package it.polimi.ingsw.client.view.gui;

import javafx.fxml.FXML;
import javafx.scene.control.Label;
import javafx.scene.control.TableColumn;
import javafx.scene.control.TableView;
import it.polimi.ingsw.client.view.gui.GuiData;
import it.polimi.ingsw.client.ClientMainR;

public class GuiInitController {

/*    @FXML
    private UserNameColumn <String> userName;

    public GuiInitController() {
    }

    @FXML
    private void initialize() {
        // Initialize the person table with the two columns.
       UserNameColumn.setCellValueFactory(cellData -> cellData.getValue().userNameProperty());
    }

    public void setGuiData(GuiData guiData) {
        this.guiData = guiData;

        // Add observable list data to the table
        personTable.setItems(guiData.getPersonData());
    }*/

}
